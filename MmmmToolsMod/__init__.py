"""MmmmToolsModNew top level folder for MmmmTools, because the clashing namesbetween classes and modules were causing major problems before!"""## Imports must come first, on their own, without any other## code happening or running## otherwise  modules that import each other## in circular fashions will fail## (we take advantage normally of python's somewhat##  magic circular importing, but we have to be disciplined)#### imports will be relative if in same folder## (Python checks relative path first before global)#### if it's a module here directly, use it unqualified## otherwise be sure to qualify it properly####try:    reload(unipath)except:    import unipathtry:    reload(Static)except:    import Static    try:    reload(Dynamic)except:    import Dynamic"""try:    reload( MmmmToolsMod.unipath )except:    import MmmmToolsMod.unipath as unipathtry:    reload( MmmmToolsMod.Static )except:    import MmmmToolsMod.Statictry:    reload( MmmmToolsMod.Dynamic )except:        import MmmmToolsMod.Dynamic    """"""no_auto = False    if not no_auto:    try:        reload( MmmmToolsMod.Dynamic )    except:"""